<UserControl x:Class="Protsenko.TheoryEditor.VisualElements.OptionsControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Height="20" Width="100">
    <Grid>
        <Grid.Resources>
            <Style TargetType="{x:Type Control}">
                <Setter Property="Margin" Value="5"/>
            </Style>
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <!--<TreeView Grid.Column="0">
            
        </TreeView>-->
        <Button Name="OK" Content="OK" Click="OK_Click"/>
        <ComboBox Name="Languages" Grid.Column="1" SelectionChanged="Languages_SelectionChanged"/>
    </Grid>
</UserControl>
